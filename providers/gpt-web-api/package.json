{
  "name": "gpt-web-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "find . -name '*.mjs' -not -path './node_modules/*' -print0 | xargs -0 -n1 node --check",
    "test": "node --test test/*.test.mjs",
    "start": "node server.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
