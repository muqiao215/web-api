# gpt-web-api

Future target for `/root/.ductor/workspace/gpt_web_api`.

Current live unit:

- `gpt-web-api.service`
- bind: `127.0.0.1:4242`
- current entry: `/root/.ductor/workspace/gpt_web_api/server.mjs`

Migration rule: move only after `gpt-web-responses` shim and `sub2api` account tests still pass.
