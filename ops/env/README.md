# Env Templates

Only non-secret examples belong here.

Real secrets should stay in host-level secret stores such as `/root/.ductor/.env` or root-owned `/etc/genapi/*.env` files. Do not commit API keys, OAuth tokens, cookies, or browser profile archives.
