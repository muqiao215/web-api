# Env Templates

Only non-secret examples belong here.

Real secrets should stay in a host-level secret store such as a root-owned env file, secret manager, or service-specific credential store. Do not commit API keys, OAuth tokens, cookies, or browser profile archives.
