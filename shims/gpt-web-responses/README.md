# gpt-web-responses

Future target for `/root/.ductor/workspace/provider_shims/gpt_web_responses_shim.mjs`.

Current live unit:

- `gpt-web-responses-shim.service`
- bind: `127.0.0.1:4252`
- upstream: `http://127.0.0.1:4242`

This shim is what lets `gpt_web_api` fit the `sub2api` OpenAI/Responses path.
