{
  "name": "web-capability-api",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/control-workbench",
    "apps/private-worker",
    "packages/provider_artifacts",
    "providers/gpt-web-api",
    "providers/catgpt-gateway",
    "providers/gemini-web",
    "providers/canvas-to-api",
    "providers/qwen2api/frontend",
    "shims/chat-responses",
    "shims/gpt-web-responses",
    "consumers/showcase-web"
  ],
  "scripts": {
    "gemini:start": "node providers/gemini-web/start.mjs",
    "gemini:start:compat": "node providers/canvas-to-api/start.mjs",
    "gemini:runtime:status": "node providers/gemini-web/runtime_status.mjs",
    "doctor": "uv run --project packages/ops_doctor wcapi-doctor",
    "doctor:strict": "uv run --project packages/ops_doctor wcapi-doctor --strict",
    "check:layout": "test -f README.md && test -d providers && test -d shims && test -d packages && test -d ops",
    "prompt:sync": "uv run --project packages/prompt-factory prompt-factory sync",
    "prompt:build": "uv run --project packages/prompt-factory prompt-factory build --profile all",
    "prompt:diff": "uv run --project packages/prompt-factory prompt-factory diff --profile local_repos",
    "prompt:promote": "uv run --project packages/prompt-factory prompt-factory promote --profile local_repos"
  }
}
