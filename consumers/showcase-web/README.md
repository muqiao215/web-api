# showcase-web

Future target for local showcase sites.

This should consume provider APIs and shared assets. It should not contain provider runtime code.

This workspace consumer is not the same thing as the independent
`/root/.ductor/workspace/telegram_canvas_bot/showcase_web` project.
