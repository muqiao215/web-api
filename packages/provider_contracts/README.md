# provider_contracts

Shared JSON schemas for provider capability and runtime health.

These contracts are intentionally small. They should standardize what the control plane and doctor need to know without forcing every provider into the same implementation.
